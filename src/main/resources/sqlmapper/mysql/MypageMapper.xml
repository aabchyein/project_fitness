<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mypage">
<!-- 회원상세정보 -->
    <select id="MypageDetail" parameterType="Map" resultType="Map">   
     SELECT NAME, ID, PASSWORD, BIRTHDATE, PHONE_NUMBER, EMAIL_ADDRESS, ADDRESS, GENDER 
     FROM member_information as meminfor
     INNER JOIN gender as gen
        on meminfor.GENDER_ID = gen.GENDER_ID
      AND ID like 'admin123'
        ;
    </select>
</mapper>